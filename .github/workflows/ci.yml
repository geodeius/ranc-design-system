name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.17.0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check formatting
        run: pnpm format:check

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

      - name: Validate content
        run: pnpm validate:content

      - name: Validate internal links
        run: pnpm validate:links

      - name: Validate registries
        run: pnpm validate:registries

      - name: Validate AI context
        run: pnpm validate:ai

      - name: Validate package release intent
        run: pnpm validate:changesets

      - name: Build
        run: pnpm build

      - name: Install accessibility test browser
        run: pnpm --filter @rancard/docs exec playwright install --with-deps chromium

      - name: Check documentation accessibility
        run: pnpm accessibility:check

      - name: Validate workspace
        run: pnpm validate:workspace

      - name: Validate Changesets configuration
        run: pnpm changeset:status
